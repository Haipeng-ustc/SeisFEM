double mesh_xy_t4 ( int nx, int ny, int node_num, double xmin, double xmax, double ymin, double ymax, double **node_xy)

/******************************************************************************/
/*

 grid_t3_xy sets the XY coordinates of the nodes.


  Parameters:

    Input, integer ( kind = 4 ) NX, NY, the number of elements in the X and
    Y direction.

    Input, integer ( kind = 4 ) NODE_NUM, the number of nodes.

    Input, real ( kind = 8 ) xmin, xmax, zmin, zmax, the X coordinates of
    the left and right sides of the rectangle, and the Y coordinates
    of the bottom and top of the rectangle.

    Output, real ( kind = 8 ) node_xy(2,NODE_NUM),
    the coordinates of the nodes.

*/
{
  int i;
  int j;

for(j=0; j < 2 * ny + 1;j++)
{
    for(i=0; i < 2 * nx + 1;i++)
    {
        node_xy[0][ i + j * (2 * nx + 1)] = i * ( xmax - xmin ) / (2.0 * nx);
        node_xy[1][ i + j * (2 * nx + 1)] = j * ( ymax - ymin ) / (2.0 * ny);
    }
}


}


